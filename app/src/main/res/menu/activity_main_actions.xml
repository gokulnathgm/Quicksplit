<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"  android:layout_height="20dip"
    android:layout_width="fill_parent">

    <!-- New expense -->
    <item android:id="@+id/action_new"
        android:icon="@drawable/ic_action_new"
        android:title="New Expense"
        android:showAsAction="always"/>

    <!-- Add new person -->
    <item android:id="@+id/action_add_friend"
        android:icon="@drawable/ic_action_add_person"
        android:title="New Friend"
        android:showAsAction="always" />

    <item android:id="@+id/action_all_expenses"
        android:icon="@drawable/ic_action_view_as_list"
        android:title="All Expenses"
        android:showAsAction="always" />

</menu>